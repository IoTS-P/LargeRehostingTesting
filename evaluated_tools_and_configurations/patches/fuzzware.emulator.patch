diff --git a/harness/fuzzware_harness/native/native_hooks.c b/harness/fuzzware_harness/native/native_hooks.c
index f9e565a..c792303 100644
--- a/harness/fuzzware_harness/native/native_hooks.c
+++ b/harness/fuzzware_harness/native/native_hooks.c
@@ -1104,7 +1104,7 @@ uc_err emulate(uc_engine *uc, char *p_input_path, char *prefix_input_path) {
         if(write(pipe_to_parent[1], emulate, 1) != 1) {
             puts("[Discovery Child] Error: Could not notify parent of failure..."); fflush(stdout);
         }
-        _exit(-1);
+        exit(-1);
     }
 
     // After consuming first part of input and executing the prefix, set input mode
diff --git a/setup.sh b/setup.sh
index 2318ff4..e1341ce 100755
--- a/setup.sh
+++ b/setup.sh
@@ -16,8 +16,10 @@ echo "[*] Building native harness module"
 make -C harness/fuzzware_harness/native clean all || exit 1
 
 echo "[*] Installing harness"
+
+pip3 install "setuptools<58.0.0" || exit 1
 pip3 install -U cython || exit 1
 pip3 install -U -r requirements.txt || exit 1
 
-pushd harness; pip3 install -e . || { popd; exit 1; }; popd
+pushd harness; pip3 install --no-build-isolation -e . || { popd; exit 1; }; popd
 exit 0
\ No newline at end of file
