# 字符串补充模块 —— StringAdder

## 分类

- 通用

## 描述

查找未被 Ghidra 定义的字符串。

## 补充解释

使用 Ghidra 的字符串搜索 API 来搜索所有 ASCII 字符序列。默认情况下，我们会搜索：
- 长度大于 5 的字符串，以 `'\0'` 结尾
- 包含 UNIX 控制字符的字符串，如 `'\x1b[0;31m'`

有时还会存在一些长度小于 5 的字符串，无法被 Ghidra 识别，我们会探索两个字符串之间的空隙，并尝试识别为较小的字符串。